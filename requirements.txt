# the web framework:
Flask==3.1.3
Jinja2==3.1.6

# database stuff:
peewee==4.1.2
# for importing 'external data' sources:
feedparser==6.0.12
bleach==6.4.0
# a lot of our modules are separately pulled in later:
#importlib
# for passwords:
bcrypt==5.0.0
# for parsing date-related formulae:
simpleeval==1.0.7
# simple python-based WSGI webserver:
Werkzeug==3.1.8
waitress==3.0.2
# efficient static file serving in-process (replaces the old nginx sidecar):
whitenoise==6.12.0
# for tests:
pytest==9.1.1
coverage==7.15.0
pylint==4.0.6
html5lib==1.1
